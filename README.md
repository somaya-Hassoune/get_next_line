# get_next_line
get_next_line is a C project that consists of implementing a function capable of reading a file descriptor line by line.  This project focuses on memory management, static variables, and efficient buffer handling to ensure optimal performance and avoid memory leaks.
